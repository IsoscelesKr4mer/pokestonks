// Vitest stub for the 'server-only' package.
// Next.js uses this to prevent server modules from being imported in client bundles.
// In the test environment there is no client/server split, so this is a no-op.
export {};
